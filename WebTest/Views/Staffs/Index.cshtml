@model IEnumerable<WebTest.Models.Staff>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


@if (Session["accountRole"].ToString() == ("Admin"))
{
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>

    <table id="example1" class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Email</th>
                <th>Age</th>
                <th>Address</th>
                <th>Account</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.staffId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.staffName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.staffEmail)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.staffAge)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.staffAddress)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.accountId)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.staffId }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.staffId }, new { onclick = "return confirm('Are you sure you wish to delete this staff account?');" })
                    </td>
                </tr>
            }
        </tbody>
        <tfoot>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Email</th>
                <th>Age</th>
                <th>Address</th>
                <th>Account</th>
                <th>Action</th>
            </tr>
        </tfoot>
    </table>
}
else
{
    <div class="error-page">
        <div class="error-content">
            <h3><i class="fas fa-exclamation-triangle text-warning"></i> Oops! Something wrong.</h3>

            <p>
                You have not been authorized to see this page, you may <a href="@Url.Action("Index", "Home")">return to index</a>.
            </p>
        </div>
    </div>
}
