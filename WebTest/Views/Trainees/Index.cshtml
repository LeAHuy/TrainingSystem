@model IEnumerable<WebTest.Models.Trainee>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@if (Session["accountRole"].ToString() == ("Staff") || Session["accountRole"].ToString() == ("Trainee"))
{
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>

    <table id="example1" class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Email</th>
                <th>Age</th>
                <th>Day of birth</th>
                <th>Education</th>
                <th>Account</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.traineeId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.traineeName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.traineeEmail)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.traineeAge)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.traineeDoB)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.traineeEducation)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.accountId)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.accountId }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.accountId }, new { onclick = "return confirm('Are you sure you wish to delete this trainee account?');" })
                    </td>
                </tr>
            }
        </tbody>
        <tfoot>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Email</th>
                <th>Age</th>
                <th>Day of birth</th>
                <th>Education</th>
                <th>Account</th>
                <th>Action</th>
            </tr>
        </tfoot>
    </table>
}
else
{
    <div class="error-page">
        <div class="error-content">
            <h3><i class="fas fa-exclamation-triangle text-warning"></i> Oops! Something wrong.</h3>

            <p>
                You have not been authorized to see this page, you may <a href="@Url.Action("Index", "Home")">return to index</a>.
            </p>
        </div>
    </div>
}
